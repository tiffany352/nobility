use nobility::parser::Document;

fn main() {
    //let data = &[];

    // let root = parse_document(data);
    // for entry in root.iter() {
    //     if let Some(list) = entry.value.as_list() {
    //         for value in list.iter() {
    //             println!("list value: {}", value);
    //         }
    //     }
    //     if let Some(compound) = entry.value.as_compound() {
    //         for entry in compound.iter() {}
    //     }
    // }
}
